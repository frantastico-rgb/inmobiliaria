services:
  # 🚨 NOTA: ¡El servicio de Base de Datos ha sido eliminado de este archivo!

  # Servicio para la Aplicación Web (PHP + Apache Dockerizado)
  - type: web
    name: inmobiliaria-web
    env: docker
    dockerfilePath: ./Dockerfile

    envVars:
      # Las referencias deben apuntar al nombre del servicio de DB creado manualmente
      # y usar el tipo 'database' (que ahora sí se permite como referencia)
      - key: DB_HOST
        fromService:
          type: database # Referencia al servicio de DB creado manualmente
          name: inmobiliaria-db
          property: host
      # ... (mismo cambio para user, password, database) ...

    disk:
      name: uploads-data 
      mountPath: /var/www/html/uploads
      sizeGB: 1